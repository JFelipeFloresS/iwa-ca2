tasks:
  - name: Npm initialisation
    init: |
      npm install
      npm install nodemon mongoose
    command: npm run start
  - name: Run ReactApp
    before: cd albums
    init: npm install
    command:  npm start
